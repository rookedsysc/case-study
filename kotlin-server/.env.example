# root .env 로드 후 이 파일로 호스트만 오버라이드
# IntelliJ EnvFile 플러그인: Run Configuration → EnvFile → Files에 아래 순서로 추가
#   1. ../.env  (공통 설정: 포트, 비밀번호, DB명 등)
#   2. .env     (이 파일: docker 서비스명 → localhost 오버라이드)
MYSQL_HOST=localhost
MYSQL_SOURCE_HOST=localhost
REDIS_HOST=localhost
